pipeline {
    agent any

    stages {
        stage('compile') {
            steps {
                echo 'compile is done'
            }
        }
        stage('test') {
            steps {
                echo 'test is done'
            }
        }
        stage('build') {
            steps {
                echo 'build is done'
            }
        }
        stage('deploy') {
            steps {
                echo 'deploy is done'
            }
        }
    }
}
